### Hands On
2. Sesi FRESH (buka baru / /clear), kasih Claude task itu TANPA skill apa pun. Catat persis di mana defaultnya kurang/salah/inkonsisten (ini baseline/RED).

Bash Locate module3-project directory under mastering-claude course

Bounded task — resource nyata ada di repo, tambah CRUD Product ikut pola serupa Note. Desain singkat di chat, tanpa spec file.
Satu pertanyaan dulu: Postgres connection gimana? Repo belum ada Postgres setup (cuma in-memory buat Note sekarang).

AskUserQuestion
OUT
Your questions have been answered: "Postgres buat Product API jalan dari mana?"="H2 in-memory dulu, Postgres nanti". You can now continue with these answers in mind.
AskUserQuestion
OUT
Your questions have been answered: "PR readiness scope gimana? Bikin branch + push + PR beneran, atau code selesai + siap commit aja (user yang push/PR manual)?"="Code + commit only (Recommended)". You can now continue with these answers in mind.
Bounded change dalam project sama, Note gak disentuh.

Package baru com.mastering.claude.product:
model/Product.java — entity: id, name, price (BigDecimal), stock (Integer)
dto/ProductRequest.java, dto/ProductResponse.java — request divalidasi (@NotBlank name, @Positive price, @Min(0) stock), response terpisah dari entity
repository/ProductRepository.java — JpaRepository<Product, Long>
service/ProductService.java — CRUD logic, lempar ProductNotFoundException kalau id gak ada
controller/ProductController.java — POST /products, GET /products/{id}, GET /products (pagination via Pageable, return Page<ProductResponse>), PUT /products/{id}, DELETE /products/{id}
exception/GlobalExceptionHandler.java + ErrorResponse DTO — @RestControllerAdvice tangani validation error (400 + field errors), not-found (404), fallback (500) — format konsisten: timestamp, status, error, message, path
DB: tambah spring-boot-starter-data-jpa + driver postgresql (runtime, buat production nanti) + h2 (runtime, dev sekarang) di pom.xml. application.properties pakai H2 mode Postgres-compatible (jdbc:h2:mem:productdb;MODE=PostgreSQL, dialect PostgreSQLDialect) — kode JPA sama persis kalau nanti pindah ke Postgres beneran, tinggal ganti connection string.

Test: unit test ProductService pakai JUnit 5 + Mockito, mock ProductRepository. Note test lama gak diubah.
Selesai: semua file jadi, mvn test pass, satu commit rapi. Push/PR manual oleh user.

3. Bikin folder d:/learn/.claude/skills/<nama-skill>/SKILL.md

name: tdd-workflow
description: "Enforce an observable test execution loop: verify a failing test before implementation and verify it passes after implementation. Use when the task requires proving RED before writing implementation code."

## Structure Template
1. RED harus benar-benar dijalankan
2. failure harus diamati
3. implementation minimum
4. GREEN harus dijalankan
5. REFACTOR setelah green

## Existing Tests

When tests already exist, do not assume they represent a valid RED phase.
Before implementing the requested behavior, run the relevant test and identify
the specific behavior that is missing or incorrect.

If the test already passes before implementation, do not claim TDD RED-GREEN
was followed. First identify whether the implementation is already complete;
if it is complete, report that no implementation is needed rather than
pretending a TDD cycle occurred.

4. Sesi baru lagi. Test trigger POSITIF: kasih task yang mestinya manggil skill-nya. Catat: ke-load gak, hasilnya beda dari baseline gak.
prompt : "Sesi FRESH (buka baru / /clear), kasih Claude task itu TANPA skill apa pun. Catat persis di mana defaultnya kurang/salah/inkonsisten (ini baseline/RED)."
log :
Globpattern: ".claude/skills/**"
Found 1 file
Custom skill. Use it.
tdd-workflow skill
Globpattern: "playground/mastering-claude/module3-project/src/**/customers/**"
Globpattern: "playground/mastering-claude/module3-project/src/main/resources/**"
Globpattern: "playground/mastering-claude/module3-project/mvnw*"
Bash Check Maven availability
Bash Run createThenGetReturnsCustomer test, expect RED
Bash Run createThenGetReturnsCustomer test, expect GREEN
Bash Check test summary line
Bash Rerun test capturing real exit code
Bash Rerun test, capture exit code to scratchpad
Bash Run listThenDeleteRemovesCustomer, expect RED
Bash Run listThenDeleteRemovesCustomer, expect GREEN
Bash Run updateCustomerReturnsUpdatedFields, expect GREEN
Bash Run updateWhenNotFoundReturnsNotFound, expect GREEN (covered by same impl)
GREEN. Next: validation tests (invalid email, missing name). Check RED.
Bash Run validation tests, expect GREEN or RED status
Full suite (Customer + Note) all green, no leftover regressions.
Customer CRUD done, RED-GREEN-REFACTOR per behavior:
create+getById: RED (404) → GREEN
list+delete: RED (parse error) → GREEN
update+update-not-found: RED (expected 200 got 404) → GREEN
validation (email/name blank), getById-not-found, list-empty, delete-not-found: already covered, verified GREEN, no refactor needed.
All 8 tests + existing Note tests pass (mvn test, exit 0).

5. Test trigger NEGATIF: kasih task mirip topik tapi harusnya SKILL INI GAK RELEVAN. Catat: salah kepicu gak.
prompt : "Test sudah tersedia. Implementasikan Customer API sampai pass"
log :
superpowers:test-driven-development skill
Bash List Java files in module3-project
No Customer test file found repo-wide. Ask user where test lives.
AskUserQuestion


### Interview Questions (mikir dulu, jangan buka referensi)
1. Kenapa testing skill wajib di sesi baru — bukan sesi tempat nulis? Kaitkan ke mekanisme loading Level 1 vs Level 2.
   Karena yang sedang diuji adalah mekanisme loading skill, bukan kemampuan Claude mengikuti instruksi yang baru saja kita tulis. kalau di session yang sama bisa missleading karena model sudah mengetahui isi tujuan.

2. Beda "baseline test" (RED) di skill authoring vs unit test biasa di TDD kode — apa yang difailkan masing-masing?
   Keduanya sama-sama menggunakan prinsip failure sebagai bukti, tetapi yang diuji berbeda.

3. Staff-level: gimana kamu tau kapan skill butuh file references/ terpisah vs cukup ditulis inline di body? Sebut sinyal konkret.
   Dengan melihat apakah context utama sedang terbebani oleh knowledge yang tidak selalu diperlukan. contoh Isinya panjang tetapi tidak selalu dibutuhkan. Idealnya Level 2 pertama kali mendapatkan minimum instruction yang diperlukan untuk mengambil keputusan, kemudian detail dimuat saat diperlukan.