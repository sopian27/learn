1. Kalau kamu kerja di project yang cuma butuh Linear, tapi ada 6 MCP server lain nyala default — apa dampak konkretnya ke context window, dan langkah apa yang kamu ambil?
    6 mcp server itu tetep di load di awal, mesikpun yang digunakan hanya 1, akibatnya token jadi membengkak meskipun prompt nya sudah spesifik, langkah yang harus diambil adalah disable mcp server yang tidak digunakan.

Jelasin kenapa MCP tool result dianggap "untrusted data" — kaitkan ke risiko prompt injection lewat data eksternal (misal isi email atau deskripsi issue).
    karena berasal dari sistem external, harus di verifikasi terlebih dahulu.

Bandingkan: bikin custom tool lewat MCP server vs bikin Skill biasa (folder instruksi) — kapan masing-masing lebih make sense?
    apabila task itu sudah bisa di selesaikan via skill tidak harus membuat mcp server, tapi kalau memang skill tidak bisa mengatasinya
    boleh ditambahkan mcp server, tapi dengan pertimbangan.