### next
1. Copy description pendek dari draft ke file asli .claude/skills/tdd-workflow/SKILL.md.
   sudah saya update

2. Sesi baru, ulang positive test, lanjutin sampe kebukti RED-run-gagal → GREEN-run-lolos eksplisit di log.
   tdd-workflow skill
   Grep "class NoteController" (in d:\learn\playground\mastering-claude\module3-project)
   3 lines of output
   Grep "delete" (in d:\learn\playground\mastering-claude\module3-project)
   3 lines of output
   Read d:\learn\playground\mastering-claude\module3-project\src\test\java\com\mastering\claude\notes\NoteControllerTest.java, d:\learn\playground\mastering-claude\module3-project\src\main\java\com\mastering\claude\notes\controller\NoteController.java
   Read d:\learn\playground\mastering-claude\4.2.md, d:\learn\playground\mastering-claude\4.2.fix.md
   Grep "[Nn]ull" (in d:\learn\playground\mastering-claude\module3-project\src\test)
   3 lines of output
   Read d:\learn\note.txt
   Globpattern: "playground/mastering-claude/module3-project/mvnw*"
   No files found
   Bash Check Maven availability
   Maven ada. Tulis test RED dulu — direct unit call delete(null), expect 400.
   Sekarang jalankan test itu, buktikan RED.
   Bash Run only the new null-id delete test
   RED terbukti: store.remove(null) lempar NullPointerException di ConcurrentHashMap.remove — id=null belum ditangani. Implementasi guard clause sekarang.
   Jalankan test sama lagi, buktikan GREEN.
   Bash Rerun null-id delete test, expect GREEN
   Bash Check previous command exit code
   Bash Rerun test capturing real exit code
   GREEN, exit 0. Full suite sekarang, cek regresi.
   Bash Run full test suite, check for regressions
   Full suite: 16 test (Note 7 + Customer 9), semua pass, exit 0. No regresi.
   Ringkasan:
   RED terbukti nyata: store.remove(null) di NoteController.java:53 lempar NPE dari ConcurrentHashMap (key null tak diizinkan) — test deleteWithNullIdReturnsBadRequest gagal (Error, bukan Failure).
   Fix minimal: guard clause if (id == null) return badRequest() sebelum store.remove(id).
   GREEN: test lulus sendiri, dan full suite (16 test) tetap hijau — no refactor perlu, perubahan sudah minimal.

3. Negative test: tambah 1 langkah cek eksplisit skill aktif apa (bukan cuma nebak dari log).
   Review com.mastering.claude.notes.controller.NoteController.delete dan jelaskan behavior endpoint tersebut ketika menerima id null. Jangan mengubah code atau test, cukup berikan hasil analisis.
   Globpattern: "**/NoteController.java"
   Found 1 file
   Read d:\learn\playground\mastering-claude\module3-project\src\main\java\com\mastering\claude\notes\controller\NoteController.java
   Read d:\learn\playground\mastering-claude\module3-project\src\test\java\com\mastering\claude\notes\NoteControllerTest.java
   delete(id) null behavior:


   if (id == null) {
      return ResponseEntity.badRequest().build();
   }
   Guard fire → return 400 Bad Request, no body, store untouched (no remove call, no exception).

   Reachability via real HTTP: unreachable. Route DELETE /notes/{id} needs non-empty path segment to match. Empty segment → 404 (no match), non-numeric segment (e.g. /notes/abc) → Spring's Long conversion fail first, throw MethodArgumentTypeMismatchException, respond 400 before method body run — id param never null in real request, always resolved value or exception path already taken.

   Guard only reachable by direct method call, bypass DispatcherServlet — exact case in NoteControllerTest.java:91-97: new NoteController() then controller.delete(null) direct. That test pass, confirm 400 return correct for this path.

   Verdict: Guard defensive-only, dead code through actual HTTP layer, alive only through direct unit-test invocation of controller method. Not bug — just note: @PathVariable Long id never null via routed request, so null-check exist purely for direct-call safety (or future refactor where id become optional/required=false).
