1. Membaca tool_input.command
    
    import json
    import sys

    payload = json.load(sys.stdin)
    command = payload.get("tool_input", {}).get("command", "")
    
    if "rm -rf" in command:
      sys.exit(2)

    if "git push --force" in command:
      sys.exit(2)


2. Exit code apa yang kamu pakai untuk block, dan kenapa bukan exit code lain?
    sys.exit(2), karena untuk memberitahu claude bahwa hooks ini ada yang tidak beres

3. Di mana kamu daftarkan hook ini supaya Claude Code benar-benar memanggilnya — file apa, key apa?
   file : .claude/settings.json
   key  : hooks