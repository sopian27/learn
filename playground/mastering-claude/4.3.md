 ### Exercise 

plugin : D:\learn\playground\mastering-claude\plugins\tdd-toolkit
log    :   
tdd skill
Grep "class NoteController"
5 lines of output
Read d:\learn\playground\mastering-claude\module3-project\src\main\java\com\mastering\claude\notes\controller\NoteController.java
Read d:\learn\playground\mastering-claude\module3-project\src\test\java\com\mastering\claude\notes\NoteControllerTest.java
Controller delete already null-check id → BadRequest (line 52-55). Test deleteWithNullIdReturnsBadRequest already exist (line 90-97), call controller direct (unit-level, since @PathVariable Long can't be null via HTTP route). Check git status — maybe already committed or staged.

Bash Check git status of module3-project files
Validasi sudah ada, exit=0, semua test pass — termasuk deleteWithNullIdReturnsBadRequest.

Status: NoteController.delete (line 51-58) sudah cek id == null → badRequest(), dan test deleteWithNullIdReturnsBadRequest (line 90-97, NoteControllerTest.java) sudah verifikasi ini, panggil controller langsung karena @PathVariable Long id tak pernah null lewat HTTP route asli (segmen path kosong → 404 sebelum masuk controller, jadi test unit-level perlu untuk cover null branch). Sudah commit di 4590a1f. Tidak ada perubahan perlu.



### Interview Questions
1. Bedain fungsi plugin.json vs marketplace.json — kalau ketuker apa akibatnya?
   plugin.json describe SATU plugin, marketplace.json describe LIST plugin (index). tidak akan success di load oleh claude

2. Kenapa hook command wajib pakai ${CLAUDE_PLUGIN_ROOT} bukan path absolut?
   karena target machine instal berbeda beda setiap komputer

3. Skill di plugin vs skill manual di .claude/skills/ — beda cara Claude Code resolve/load-nya?
   ada perbedaan dari resolve path nya dan proses instalasi 

4. Kalau plugin sama ke-install 2x scope beda (user vs local), mana yang menang?
   local scope akan menang dan override user scope