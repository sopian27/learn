# 7.3 â€” Fungsi kondisional & agregasi kondisional: `CASE WHEN` di dalam `SUM`/`COUNT` (pivot manual), `GREATEST()`/`LEAST()`, `COALESCE()`/`NULLIF()` untuk data yang tidak lengkap — pola yang Anda temukan langsung di kode teman Anda

## Objective

## Materi

## Exercise

## Notes

## Applied Practice

## Interview Questions

## Review
