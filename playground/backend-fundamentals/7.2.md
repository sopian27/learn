# 7.2 â€” Window function: `ROW_NUMBER()`, `RANK()`/`DENSE_RANK()`, `LAG()`/`LEAD()`, `SUM()`/`AVG() OVER (PARTITION BY ... ORDER BY ...)` — inti dari hampir semua laporan (running total, top-N per grup, perbandingan periode)

## Objective

## Materi

## Exercise

## Notes

## Applied Practice

## Interview Questions

## Review
